
spring:
  application:
    name: spring-boot-data-jpa-hospital-management-system
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 10July@1990
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
